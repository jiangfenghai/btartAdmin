import React from "react";
import "./style.scss";

export const CheckRingDuotone = ({ className }) => {
  return (
    <img
      className={`check-ring-duotone ${className}`}
      alt="Check ring duotone"
      src="/img/check-ring-duotone.png"
    />
  );
};
